module.exports = {
  root: true,
  extends: '@react-native',
  rules: {
    'react-native/no-inline-styles': 'off', // Disable the rule
    'react-hook/exhaustive-deps': 'off', // Disable the rule
  },
};
